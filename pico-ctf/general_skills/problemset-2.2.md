
## First Grep
Used Grep to find the picoCTF pattern for the flag
```
grep "picoCTF" file

```

## First find
Used the find command to locate the file and cat to show the flag.
```
find ./ -name "uber-secret.txt"
```

## Big Zip
Downloaded the file, unzipped it, used ``grep "picoCTF" -r`` and boom! Got the flag.
